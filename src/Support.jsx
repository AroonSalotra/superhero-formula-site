const Support = (props) => {
    return (
        <div className="banner">
            <img src={props.imgUrl} alt="" />
            <p>COMPANY SUPPORT TEXT</p>
        </div>
    )
}

export default Support